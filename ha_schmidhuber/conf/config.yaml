defaults:
    - profile: full
    - _self_

trackio:
    write_url: ??
    project: 'ha_schmidhuber'
    run_name: 'vae{beta=1,epochs=1,episodes=1k,freebits=false}'
rnn:
    hidden_size: 256
    num_mix: 5
    z_dim: 32
    action_dim: 3
    temp: 0.2
controller:
    state_dim: 32
    hidden_dim: 256
    action_dim: 3
device: 'mps'
